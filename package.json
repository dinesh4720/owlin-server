{
  "name": "owlin-analytics-server",
  "version": "2.0.0",
  "description": "Owlin Analytics — Mixpanel-like event collection, error tracking, and analytics server",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "node dist/index.js",
    "dev": "tsx --watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "socket.io": "^4.8.1",
    "typescript": "^5.7.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
